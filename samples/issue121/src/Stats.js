import {getCount} from "./count.js";
import {getDuration} from "./duration.js";

export default class {
    getCount() {
        return getCount();
    }

    getDuration() {
        return getDuration();
    }
}
